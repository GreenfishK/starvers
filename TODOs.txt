Implement support for multiple RDF serializations including file archives and directories with multiple RDF files.

Investigate why import fails for for https://bimerr.iot.linkeddata.es/def/weather/ontology.nt

Implement client to pass a URL with an ontology behind to start the versioning

timings_csv: add headers: done

Implement logs: done

Implement stop versioning

Resume service: If the repository exists, the tracking should be ressumed after a container shutdown or even deletion

Persist Postegresql Database

A service that iterates through all zip files, 
calculates deltas between the consecutive snapshots 
and inserts them with the corresponding timestamp