SELECT ?same_name_label
WHERE {
  ?resources rdfs:label ?resources_labels.
  FILTER(REGEX(?resources_labels, "Bisphenol A", "i"))
  ?resources orkgp:SAME_AS ?same_name.
  ?same_name rdfs:label ?same_name_label.
}
